# 云智新闻 (CloudMind News)

一个基于 FastAPI、LangGraph 和 ChromaDB 构建的智能化新闻系统，集成 AI Agent 实现智能问答、实时搜索和知识库检索功能。

## 1-1 项目概述

这是一个仿今日头条的智能新闻系统后端 API 服务，采用异步 Python 框架 FastAPI 开发，使用 MySQL 作为主数据库，ChromaDB 作为向量数据库，Redis 作为缓存层。系统不仅提供传统的用户管理、新闻浏览、收藏和历史记录功能，还创新性地集成了基于 LangGraph 的 AI Agent，支持多轮对话、联网搜索和本地知识库检索，为用户提供智能化的新闻解读与问答服务。

## 1-2 技术栈

### 核心框架
- **后端框架**: FastAPI (异步 Web 框架)
- **数据库 ORM**: SQLAlchemy (Async) + aiomysql
- **向量数据库**: ChromaDB (PersistentClient)
- **缓存系统**: Redis (redis.asyncio)
- **AI 框架**: LangGraph (Agent 工作流编排)

### AI 相关
- **大语言模型**: 阿里云百炼 - Qwen3.6-flash
- **Embedding 模型**: text-embedding-v3
- **联网搜索**: Tavily Search API
- **向量嵌入**: 自定义 EmbeddingService

### 安全与认证
- **密码加密**: passlib + bcrypt
- **身份认证**: JWT (JSON Web Token)
- **异步支持**: Python asyncio

## 1-3 项目结构

```
toutiao_backend/                          # 项目根目录
├── agents/                              # AI Agent 核心模块
│   ├── graph.py                       # LangGraph 工作流定义
│   ├── nodes.py                       # Agent 节点函数（路由、搜索、检索、生成）
│   ├── state.py                       # Agent 状态定义 (Pydantic)
│   └── tools.py                       # Agent 工具集（搜索、检索、时间）
│
├── cache/                               # 缓存层封装
│   └── news_cache.py                  # 新闻缓存策略实现
│
├── config/                              # 配置管理
│   ├── ai_conf.py                     # AI 相关配置（API Key、模型、Chroma、Redis）
│   ├── db_conf.py                     # 数据库配置
│   └── cache_conf.py                  # Redis 缓存配置
│
├── crud/                                # 数据访问层（CRUD操作）
│   ├── chat.py                        # AI 会话与消息 CRUD
│   ├── favorite.py                    # 收藏相关数据库操作
│   ├── history.py                     # 历史记录相关数据库操作
│   ├── news.py                        # 新闻相关数据库操作
│   ├── news_cache.py                  # 新闻缓存操作
│   └── users.py                       # 用户相关数据库操作
│
├── models/                              # 数据模型定义 (SQLAlchemy ORM)
│   ├── chat.py                        # AI 会话与消息模型
│   ├── favorite.py                    # 收藏数据模型
│   ├── history.py                     # 历史记录数据模型
│   ├── news.py                        # 新闻数据模型
│   └── users.py                       # 用户数据模型
│
├── routers/                             # API路由定义
│   ├── ai_chat.py                     # AI 聊天接口
│   ├── favorite.py                    # 收藏相关API路由
│   ├── history.py                     # 历史记录相关API路由
│   ├── news.py                        # 新闻相关API路由
│   └── users.py                       # 用户相关API路由
│
├── schemas/                             # 数据验证模型（Pydantic）
│   ├── favorite.py                    # 收藏数据验证模型
│   ├── history.py                     # 历史记录数据验证模型
│   ├── news.py                        # 新闻数据验证模型
│   └── users.py                       # 用户数据验证模型
│
├── services/                            # 业务服务层
│   ├── chroma_service.py              # ChromaDB 向量数据库服务
│   ├── embedding_service.py           # Embedding 向量化服务
│   ├── memory_service.py              # Redis 对话记忆服务
│   ├── summary_service.py             # 新闻摘要服务
│   └── tavily_service.py              # Tavily 联网搜索服务
│
├── utils/                               # 工具函数
│   ├── auth.py                        # JWT 认证工具
│   ├── exception.py                   # 自定义异常类
│   ├── exception_handler.py           # 全局异常处理器
│   ├── response.py                    # 统一响应格式
│   └── security.py                    # 安全工具（密码加密）
│
├── chroma_data/                         # ChromaDB 持久化数据目录
├── milvus_data/                         # （已废弃，保留用于迁移）
├── main.py                              # 应用入口文件
├── index_news.py                        # 新闻向量化索引脚本
└── .env                                 # 环境变量配置文件
```

## 1-4 功能模块

### 1. 用户管理模块

- 用户注册（JWT Token 发放）
- 用户登录（密码 Bcrypt 验证）
- 用户信息获取与更新
- 用户密码修改
- Token 有效期管理（7天）

### 2. 新闻模块

- 新闻分类获取（支持缓存）
- 新闻列表获取（分页、分类筛选、缓存优化）
- 新闻详情获取（自动浏览量 +1、缓存加速）
- 相关新闻推荐（基于向量相似度）

### 3. 收藏模块

- 添加/取消收藏
- 收藏列表获取（分页）
- 清空所有收藏
- 检查收藏状态

### 4. 浏览历史模块

- 自动添加浏览记录
- 浏览历史列表获取（分页）
- 删除单条浏览记录
- 清空浏览历史

### 5. AI 智能问答模块（核心亮点）

- **智能路由**：基于 LLM 自动判断问题类型（实时新闻/本地知识/闲聊）
- **联网搜索**：集成 Tavily API，获取最新时事新闻
- **知识库检索**：基于 ChromaDB 向量检索本地新闻数据库
- **多轮对话**：支持上下文理解，保持对话连贯性
- **来源标注**：回答自动标注信息来源（URL 或本地数据库）
- **会话管理**：支持多会话创建、历史查询、会话删除
- **双层记忆**：Redis 短期记忆（最近 20 条）+ MySQL 长期持久化

### 6. 缓存模块

- 新闻详情缓存（TTL: 30分钟）
- 新闻列表缓存（TTL: 10分钟）
- 分类数据缓存（TTL: 2小时）
- 用户历史记录缓存（TTL: 1小时）
- AI 对话上下文缓存（TTL: 24小时）

## 1-5 数据库设计

### 主要数据表

#### 1. **用户表 (user)**
- 用户基本信息存储
- 字段：id, username, password (bcrypt加密), nickname, avatar, gender, bio, phone, created_at, updated_at

#### 2. **用户令牌表 (user_token)**
- 用户认证令牌管理
- 字段：id, user_id, token, expires_at, created_at
- 支持令牌过期机制（7天有效期）

#### 3. **新闻分类表 (news_category)**
- 新闻分类信息
- 字段：id, name, sort_order, created_at, updated_at

#### 4. **新闻表 (news)**
- 新闻内容存储
- 字段：id, title, description, content, image, author, category_id, views, publish_time, created_at, updated_at

#### 5. **收藏表 (favorite)**
- 用户收藏记录
- 字段：id, user_id, news_id, create_time
- 关联用户和新闻，唯一约束 (user_id, news_id)

#### 6. **浏览历史表 (history)**
- 用户浏览历史记录
- 字段：id, user_id, news_id, view_time
- 关联用户和新闻

#### 7. **AI 会话表 (chat_session)** （新增）
- AI 对话会话管理
- 字段：id, user_id, session_id (UUID), title, created_at, updated_at, is_deleted
- 支持软删除，按用户隔离

#### 8. **AI 消息表 (chat_message)** （新增）
- AI 对话消息存储
- 字段：id, session_id, role (user/assistant), content, sources (JSON), question_type, created_at
- sources 存储回答的来源信息（URL 或本地 ID）

## 1-6 缓存设计

### 缓存策略

系统采用 Redis 作为缓存层，对高频访问的数据进行缓存，以提升系统性能和响应速度。

### 缓存数据类型

#### 1. **新闻详情缓存**
- 缓存键: `news:detail:{news_id}`
- 过期时间: 30分钟 (DETAIL_CACHE_TTL)
- 提升新闻详情页面访问速度

#### 2. **新闻列表缓存**
- 缓存键: `news:list:{category_id}:{page}:{size}`
- 过期时间: 10分钟 (NEWS_CACHE_TTL)
- 减少首页和分类页面的数据库查询压力

#### 3. **分类数据缓存**
- 缓存键: `news:categories`
- 过期时间: 2小时
- 加速导航栏分类数据加载

#### 4. **用户历史记录缓存**
- 缓存键: `history:list:{user_id}`
- 过期时间: 1小时
- 提高用户历史记录访问性能

#### 5. **AI 对话上下文缓存** （新增）
- 缓存键: `chat:{session_id}`
- 过期时间: 24小时 (CONVERSATION_TTL)
- 存储最近 20 条对话消息（JSON 格式）
- 支持快速读取对话历史，降低数据库压力

### 缓存更新机制

- 数据更新时自动清除相关缓存
- 采用缓存失效而非主动更新策略
- 支持批量清除特定模式的缓存
- AI 对话消息同时写入 Redis 和 MySQL，保证数据一致性

### 缓存配置

在 ai_conf.py 中配置 Redis 连接信息：

```python
REDIS_HOST = "localhost"
REDIS_PORT = 6379
REDIS_DB = 0
CONVERSATION_TTL = 86400  # 24小时
MAX_HISTORY_ROUNDS = 10   # 最多10轮对话
```

## 1-7 API 接口说明

### 用户相关接口

| 接口                 | 方法 | 说明         |
| -------------------- | ---- | ------------ |
| `/api/user/register` | POST | 用户注册     |
| `/api/user/login`    | POST | 用户登录     |
| `/api/user/info`     | GET  | 获取用户信息 |
| `/api/user/update`   | PUT  | 更新用户信息 |
| `/api/user/password` | PUT  | 修改用户密码 |

### 新闻相关接口

| 接口                   | 方法 | 说明             |
| ---------------------- | ---- | ---------------- |
| `/api/news/categories` | GET  | 获取新闻分类列表 |
| `/api/news/list`       | GET  | 获取新闻列表     |
| `/api/news/detail`     | GET  | 获取新闻详情     |

### 收藏相关接口

| 接口                   | 方法   | 说明             |
| ---------------------- | ------ | ---------------- |
| `/api/favorite/check`  | GET    | 检查新闻收藏状态 |
| `/api/favorite/add`    | POST   | 添加收藏         |
| `/api/favorite/remove` | DELETE | 取消收藏         |
| `/api/favorite/list`   | GET    | 获取收藏列表     |
| `/api/favorite/clear`  | DELETE | 清空所有收藏     |

### 浏览历史相关接口

| 接口                               | 方法   | 说明             |
| ---------------------------------- | ------ | ---------------- |
| `/api/history/add`                 | POST   | 添加浏览记录     |
| `/api/history/list`                | GET    | 获取浏览历史列表 |
| `/api/history/delete/{history_id}` | DELETE | 删除单条浏览记录 |
| `/api/history/clear`               | DELETE | 清空浏览历史     |

### AI 聊天相关接口 （新增）

| 接口                              | 方法   | 说明             |
| --------------------------------- | ------ | ---------------- |
| `/api/ai/chat`                    | POST   | AI 智能问答      |
| `/api/ai/chat/new-session`        | POST   | 创建新会话       |
| `/api/ai/chat/sessions`           | GET    | 获取会话列表     |
| `/api/ai/chat/history/{session_id}` | GET    | 获取会话历史     |
| `/api/ai/chat/history/{session_id}` | DELETE | 删除会话         |


## 1-8 AI Agent 架构设计（核心）

### 1-8-1 Agent 工作流概述

系统采用 **LangGraph** 框架构建状态机驱动的 AI Agent，实现智能化的问题处理流程。Agent 能够根据用户问题的类型自动选择最优的处理策略：联网搜索、本地知识库检索或直接回答。

### 1-8-2 Agent 状态定义 (AgentState)

Agent 工作流中传递的状态包含以下关键信息：

```python
class AgentState(BaseModel):
    session_id: str                      # 会话 ID
    user_input: str                      # 用户当前输入
    chat_history: List[Dict]             # 对话历史（最近 5 轮）
    question_type: Literal[...]          # 问题类型分类结果
    search_results: List[Dict]           # 联网搜索结果
    retrieval_results: List[Dict]        # 本地检索结果
    context: str                         # 整合后的上下文
    answer: str                          # AI 生成的最终答案
    sources: List[Dict]                  # 信息来源列表
    error: Optional[str]                 # 错误信息
```

### 1-8-3 Agent 节点详解

#### 1. **Router Node（路由节点）**

**功能**：使用 LLM 对用户问题进行智能分类

**分类类型**：
- `realtime`：询问最新新闻、时事、当前情况 → 触发联网搜索
- `knowledge`：询问历史新闻、特定主题、数据库信息 → 触发本地检索
- `chat`：问候、闲聊、与新闻无关 → 直接生成回答

**实现逻辑**：
```python
# 构建提示词，让 LLM 判断问题类型
prompt = f"""你是一个智能问题分类器。请分析用户的问题...
用户问题：{state.user_input}
对话历史：{json.dumps(state.chat_history[-3:])}"""

# 调用通义千问 API
response = Generation.call(model=DASHSCOPE_MODEL, messages=[...])

# 解析 JSON 输出，返回 question_type
return {"question_type": result.get("question_type", "chat")}
```

#### 2. **Search Node（搜索节点）**

**触发条件**：question_type == "realtime"

**功能**：调用 Tavily API 进行联网搜索

**实现逻辑**：
```python
results = await SearchTool.execute(
    query=state.user_input,
    max_results=5
)
return {"search_results": results}
```

**返回数据结构**：
```json
[
  {
    "title": "新闻标题",
    "url": "https://...",
    "content": "新闻摘要...",
    "score": 0.95,
    "source": "tavily_search"
  }
]
```

#### 3. **Retrieve Node（检索节点）**

**触发条件**：question_type == "knowledge"

**功能**：从 ChromaDB 向量数据库中检索相似新闻

**实现逻辑**：
```python
results = await KnowledgeRetrievalTool.execute(
    query=state.user_input,
    top_k=5
)
return {"retrieval_results": results}
```

**检索流程**：
1. 使用 EmbeddingService 将查询文本转换为向量
2. 在 ChromaDB 中执行相似度搜索（余弦距离）
3. 返回最相似的 Top-K 条新闻及其元数据

#### 4. **Context Builder Node（上下文构建节点）**

**功能**：整合搜索/检索结果，构建 LLM 可理解的上下文

**实现逻辑**：
```python
context_parts = []
sources = []

# 处理联网搜索结果
if state.search_results:
    context_parts.append("【联网搜索结果】")
    for result in state.search_results:
        context_parts.append(f"{result['title']}\n{result['content'][:300]}")
        sources.append({"type": "web", "title": ..., "url": ...})

# 处理本地检索结果
if state.retrieval_results:
    context_parts.append("【本地知识库检索结果】")
    for result in state.retrieval_results:
        context_parts.append(f"{result['title']}\n{result['content'][:300]}")
        sources.append({"type": "local", "title": ..., "id": ...})

return {"context": "\n".join(context_parts), "sources": sources}
```

#### 5. **Generate Answer Node（答案生成节点）**

**功能**：基于上下文和对话历史，使用 LLM 生成最终回答

**系统提示词**：
```python
system_prompt = """你是一个专业的新闻AI助手。你的任务是基于提供的信息回答用户的问题。

回答规则：
1. 如果有提供【联网搜索结果】或【本地知识库检索结果】，请基于这些信息回答问题
2. 如果没有相关信息，可以用自己的知识回答，但要说明
3. **必须在回答末尾标注信息来源**：
   - 来自网络搜索：标注"来源：[标题](URL)"
   - 来自本地知识库：标注"来源：本地数据库 - [标题]"
   - 如果是常识性回答：标注"来源：AI助手常识"
4. 保持回答简洁、准确、客观
5. 如果信息不足，诚实地告诉用户
6. 使用中文回答"""
```

**调用 LLM**：
```python
response = Generation.call(
    model=DASHSCOPE_MODEL,
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": user_message}
    ]
)
return {"answer": response.output.choices[0].message.content}
```

### 1-8-4 Agent 工作流图

```
┌─────────────┐
│  User Input │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Router Node │ ──→ 判断问题类型 (realtime/knowledge/chat)
└──────┬──────┘
       │
       ├─ realtime ──→ ┌──────────────┐
       │               │ Search Node  │ ──→ Tavily 联网搜索
       │               └──────┬───────┘
       │                      │
       ├─ knowledge ─→ ┌──────────────┐
       │               │Retrieve Node │ ──→ ChromaDB 向量检索
       │               └──────┬───────┘
       │                      │
       └─ chat ──────────────→┤
                              ▼
                  ┌─────────────────────┐
                  │ Context Builder Node│ ──→ 整合搜索结果 + 检索结果
                  └──────────┬──────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │ Generate Answer Node│ ──→ LLM 生成最终回答
                  └──────────┬──────────┘
                             │
                             ▼
                      ┌──────────┐
                      │  Output  │
                      └──────────┘
```

### 1-8-5 工具集设计 (Tools)

#### 1. **SearchTool（联网搜索工具）**
- 名称：`web_search`
- 描述：搜索实时新闻和最新信息
- 实现：封装 TavilyService

#### 2. **KnowledgeRetrievalTool（知识库检索工具）**
- 名称：`knowledge_retrieval`
- 描述：从本地新闻数据库中检索相关新闻
- 实现：封装 ChromaService

#### 3. **DateTimeTool（时间工具）**
- 名称：`get_current_time`
- 描述：获取当前日期和时间
- 用途：帮助 LLM 理解时间相关的提问

### 1-8-6 记忆系统设计

#### 短期记忆（Redis）
- **存储位置**：Redis (`chat:{session_id}`)
- **存储内容**：最近 20 条对话消息（JSON 格式）
- **过期时间**：24 小时
- **优势**：快速读取，降低数据库压力

#### 长期记忆（MySQL）
- **存储位置**：MySQL (`chat_session`, `chat_message`)
- **存储内容**：完整对话历史，包括来源标注
- **优势**：持久化存储，支持历史记录查询和多设备同步

#### 双层记忆协同工作流程
```
1. 用户发送消息
   ↓
2. 从 Redis 读取最近 20 条对话历史
   ↓
3. 如果 Redis 为空，从 MySQL 加载历史
   ↓
4. 运行 Agent，生成回答
   ↓
5. 同时保存到 Redis 和 MySQL
   ↓
6. 返回回答给用户
```

### 1-8-7 向量数据库设计 (ChromaDB)

#### 集合配置
- **集合名称**：`news_embeddings`
- **数据存储路径**：`./chroma_data`
- **向量维度**：由 Embedding 模型决定（text-embedding-v3）

#### 元数据结构
```python
metadata = {
    "news_id": 123,
    "category_id": 1,
    "title": "新闻标题",
    "content": "新闻内容（前9000字符）",
    "publish_time": "2024-01-01 12:00:00",
    "author": "作者名"
}
```

#### 向量化流程
1. 从 MySQL 读取新闻数据
2. 拼接标题和内容（前 500 字符）
3. 调用 EmbeddingService 生成向量
4. 存储到 ChromaDB（ID: `news_{news_id}`）

#### 检索流程
1. 用户提问 → 生成查询向量
2. 在 ChromaDB 中执行相似度搜索
3. 返回 Top-K 条相似新闻及其元数据
4. 计算相似度分数：`similarity = 1 - distance`

### 1-8-8 会话管理设计

#### 会话生命周期
1. **创建**：用户首次发送消息时自动生成 UUID 作为 session_id
2. **激活**：每次对话刷新 Redis TTL（24小时）
3. **查询**：支持按用户获取会话列表，按 session_id 获取历史
4. **删除**：软删除（is_deleted=1），同时清除 Redis 缓存

#### 会话标题自动生成
- 如果是第一条消息，自动截取用户输入前 30 字符作为标题
- 后续可通过 API 手动更新标题

## 1-9 认证机制

系统使用基于令牌(Token)的认证机制：

1. 用户登录成功后返回访问令牌（JWT）
2. 需要认证的接口在请求头中添加 `Authorization: token值`
3. 令牌有效期为7天
4. Token 存储在 MySQL `user_token` 表中，支持过期验证

### JWT 实现细节

```python
# utils/auth.py
def create_access_token(data: dict, expires_delta: timedelta = None):
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(days=7))
    to_encode.update({"exp": expire})
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
    return encoded_jwt

def get_current_user_id(authorization: str = Header(...)) -> int:
    # 验证 Token 有效性
    # 从数据库查询 Token 是否存在且未过期
    # 返回 user_id
```

## 1-10 错误处理

系统提供统一的错误处理机制：

### 自定义异常类

```python
# utils/exception.py
class CustomException(Exception):
    def __init__(self, code: int, message: str):
        self.code = code
        self.message = message
```

### 全局异常处理器

```python
# utils/exception_handler.py
@app.exception_handler(CustomException)
async def custom_exception_handler(request, exc):
    return JSONResponse(
        status_code=exc.code,
        content={"code": exc.code, "message": exc.message, "data": None}
    )

@app.exception_handler(HTTPException)
async def http_exception_handler(request, exc):
    return JSONResponse(
        status_code=exc.status_code,
        content={"code": exc.status_code, "message": exc.detail, "data": None}
    )
```

### 错误码规范

- **401**: 用户认证失败（Token 无效或过期）
- **404**: 资源不存在
- **500**: 服务器内部错误
- **200**: 成功（业务逻辑错误通过 code 字段区分）

## 1-11 开发规范

### 代码组织

- **分层架构**：Router → CRUD → Model → Schema
- **异步优先**：所有数据库操作使用 async/await
- **统一响应格式**：所有接口返回 `{code, message, data}`
- **Pydantic 校验**：请求参数和响应数据严格类型校验

### 命名规范

- **路由前缀**：`/api/{module_name}`
- **函数命名**：snake_case（如 `get_news_list`）
- **类命名**：PascalCase（如 `VectorDBService`）
- **常量命名**：UPPER_CASE（如 `REDIS_HOST`）

### 安全规范

- 所有密码使用 Bcrypt 加密存储
- SQL 查询使用 ORM 防止注入
- Token 敏感信息不记录到日志
- CORS 配置允许跨域访问

### 缓存规范

- 缓存操作封装成独立函数（`cache/news_cache.py`）
- 缓存键命名规范：`{module}:{type}:{key}`
- 数据更新时主动清除相关缓存
- 设置合理的 TTL，避免缓存雪崩

## 1-12 性能优化

### 数据库优化

- 使用异步 SQLAlchemy 提升并发性能
- 连接池管理减少连接开销
- 合理的数据库索引设计（user_id, news_id, session_id）
- 分页查询限制最大 pageSize（100）

### 缓存优化

- Redis 缓存热点数据（新闻列表、详情、分类）
- AI 对话上下文缓存（最近 20 条）
- 多级缓存策略：Redis → MySQL → ChromaDB

### AI Agent 优化

- Embedding 向量化结果可缓存（相同文本）
- LLM 调用超时控制（避免长时间等待）
- 对话历史截断（只保留最近 5 轮用于上下文）
- 向量检索 Top-K 限制（默认 5 条）

### 异步 IO

- 所有外部 API 调用使用 async（Tavily、阿里云百炼）
- Redis 操作使用 redis.asyncio
- 数据库操作使用 AsyncSession

## 1-13 部署与运维

### 环境变量配置 (.env)

```bash
# 数据库配置
DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/toutiao

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# AI 配置
DASHSCOPE_API_KEY=sk-xxx
TAVILY_API_KEY=tvly-xxx

# Chroma 配置
CHROMA_DATA_PATH=./chroma_data
CHROMA_COLLECTION_NAME=news_embeddings

# Agent 配置
CONVERSATION_TTL=86400
MAX_HISTORY_ROUNDS=10
```

### 新闻向量化索引

```bash
# 首次运行：将所有新闻导入 ChromaDB
python index_news.py

# 清空并重新索引
python index_news.py --clear
```

### 启动服务

```bash
# 安装依赖
pip install -r requirements.txt

# 启动 FastAPI 服务
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 监控与日志

- FastAPI 内置 Swagger UI：`http://localhost:8000/docs`
- 关键操作打印日志（Agent 节点执行、缓存命中/失效）
- 错误信息统一捕获并返回友好提示

